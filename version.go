package tork

// SDKVersion is this SDK's version, recorded on every tool_result_scan
// receipt block (sdk_version) so a reader can tell which release produced a
// given scan.
const SDKVersion = "0.3.0"
